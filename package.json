{
  "name": "bootstrap-modal-deferred",
  "version": "1.0.0",
  "description": "A wrapper to the standard Bootstrap Modal component that returns a promise that will be rejected or resolved via interaction with the modal.",
  "main": "src/deferred.modal.bootstrap.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:JAAulde/bootstrap-modal-deferred.git"
  },
  "author": "Jim Auldridge <auldridgej@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "del": "^2.2.1",
    "gulp": "^3.9.1",
    "gulp-jslint": "^1.0.1",
    "gulp-load-plugins": "^1.2.4",
    "gulp-inject-version": "^1.0.1",
    "gulp-rename": "^1.2.2",
    "gulp-uglify": "^1.5.4"
  }
}
